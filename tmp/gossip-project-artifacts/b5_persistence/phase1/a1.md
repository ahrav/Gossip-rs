A1 - Implement gossip-contracts::persistence module (real code)

Goal

Replace the current placeholder crates/gossip-contracts/src/persistence/mod.rs with actual types and traits.

Types

OvidHash (32B)

DoneLedgerKey (tenant + policy + ovid)

DoneLedgerRecord (status lattice + provenance, no secrets)

FindingRecord (stable finding)

OccurrenceRecord (version + span)

ObservationRecord (policy-scoped event; includes policy_hash, run_id, shard_id, seen_at, plus safe display fields)

Hard requirements

“No secret bytes” by construction:

Secret material only represented as fixed-width hashes with redacted Debug.

No String fields sourced from content unless explicitly marked safe/redacted.

Deliverables

pub trait DoneLedger

pub trait FindingsSink

Acceptance

Downstream crates can compile using these traits without touching backend crates.
