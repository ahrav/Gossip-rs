A3 - Add ObservationId and fix naming mismatch

Goal

The system currently has FindingId + OccurrenceId. Add:

ObservationId = H(tenant, policy_hash, occurrence_id) (or equivalent canonical scheme)

Deliverables

New ID type + derivation + golden vectors.

Update docs and any code using the wrong name.

Acceptance

No one can confuse “finding identity” with “policy-scoped detection event” in code.
