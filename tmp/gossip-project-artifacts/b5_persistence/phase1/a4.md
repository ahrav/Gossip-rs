A4 - In-memory implementations under test-support feature

Goal

Deterministic simulation doubles that behave like real stores (idempotency, lattice merges), not toy mocks.

Deliverables

InMemoryDoneLedger

InMemoryFindingsSink

Fault injection hooks: “fail next N writes”, “delay”, “reorder completions”.

Acceptance

Used by simulation tests without any backend dependencies.
