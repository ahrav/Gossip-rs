//! Content-addressed identity types, canonical encoding, and domain-separated hashing.
//!
//! This module will provide the foundational ID types (`TenantId`, `FindingId`, …),
//! the [`CanonicalBytes`] trait for deterministic serialisation, and the
//! domain-separation constants used by all other boundary modules.
